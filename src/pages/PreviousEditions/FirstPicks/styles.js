import styled from 'styled-components';
import Button from '../../../components/MainButton/styles';

export const ButtonHotPicks = styled.div`
  margin: var(--gap-medium) 0;
  ${Button} {
    width: 90%;
    padding: var(--gap-medium);
    margin: 0 auto;
  }
`;

export const p = [];
